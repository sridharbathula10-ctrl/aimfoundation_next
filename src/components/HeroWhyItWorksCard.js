export default function HeroWhyItWorksCard() {
  return (
    <aside className="hero-why-card">
      <h2>Why It Works</h2>
      <p>Programmes run inside government health systems that already exist and already reach these communities. Nothing parallel is built.</p>
    </aside>
  );
}
